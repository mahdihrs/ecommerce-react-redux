import makeStyles from '@material-ui/core/styles/makeStyles';

const useStyles = makeStyles((theme) => ({
  categoriesSideMenuContainer: {

  },
  pricesSIdeMenuContainer: {
    margin: "20px auto"
  },
  subTitleSideMenu: {
    fontWeight: 700
  },
  formControlLabel: {
    width: "100%"
  }
}));

export default useStyles;
